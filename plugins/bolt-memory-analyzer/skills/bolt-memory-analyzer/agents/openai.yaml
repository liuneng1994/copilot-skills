interface:
  display_name: "Bolt Memory Analyzer"
  short_description: "Analyze large Bolt memory traces with Perfetto"
  default_prompt: "Use $bolt-memory-analyzer to capture Bolt memory, convert it to Perfetto, and explain the peak and lifecycle issues."
